# how-to-push-in-github

markdown
### 🛠️ Initial Setup

Run the following commands in your terminal to initialize the repository and push your first commit:

```bash
git init
git remote add origin https://github.com
git branch -M main
git pull origin main
git add .
git commit -m "Initial commit"
git push -u origin main

